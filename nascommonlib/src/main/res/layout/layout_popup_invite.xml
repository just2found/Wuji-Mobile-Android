<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:id="@+id/layout_list"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:fitsSystemWindows="true"
    android:gravity="center">

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_centerInParent="true"
        android:layout_margin="0dp"
        android:background="@drawable/bg_square_full_radius_light_gray"
        android:orientation="vertical">

        <ImageView
            android:id="@+id/top_dividing_line"
            android:layout_width="match_parent"
            android:layout_height="@dimen/line_common_weight"
            android:background="@color/line_tree_view" />

        <RelativeLayout
            android:layout_width="match_parent"
            android:layout_height="wrap_content">

            <ImageView
                android:id="@+id/btn_add"
                android:layout_width="35dp"
                android:layout_height="35dp"
                android:layout_alignParentEnd="true"
                android:layout_centerVertical="true"
                android:layout_marginEnd="@dimen/common_size_radius"
                android:padding="5dp"
                android:src="@drawable/selector_button_title_add" />

            <TextView
                android:id="@+id/txt_title"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:layout_centerInParent="true"
                android:ellipsize="end"
                android:padding="10dp"
                android:text="@string/tip_share_file"
                android:textColor="@android:color/black"
                android:textSize="@dimen/text_size_big" />
        </RelativeLayout>

        <include layout="@layout/layout_contact" />
    </LinearLayout>

</RelativeLayout>